int main() {
    int result = 0;

    if (5 > 3) {
        result = 1;
    } else {
        result = 0;
    }
    print(result);

    int i = 0;
    while (i < 3) {
        print(i);
        i = i + 1;
    }

    int sum = 0;
    for (int j = 0; j < 5; j = j + 1) {
        sum = sum + j;
    }
    print(sum);

    int count = 0;
    for (int k = 0; k < 3; k = k + 1) {
        if (k > 0) {
            count = count + k;
        }
    }
    print(count);

    int x = 0;
    while (x < 2) {
        if (x == 1) {
            print(100);
        }
        x = x + 1;
    }

    return 0;
}

**Résultat attendu :**
  ```
  1      (if: 5 > 3 → vrai → result = 1)
  0      (while: i=0)
  1      (while: i=1)
  2      (while: i=2)
  10     (for: sum = 0+1+2+3+4 = 10)
  3      (for+if: count = 1+2 = 3)
  100    (while+if: quand x==1, print marker=100)